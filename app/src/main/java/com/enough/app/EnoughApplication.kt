package com.enough.app

import android.app.Application

/**
 * Application entry point. Kept intentionally thin for the Phase 0 scaffold;
 * the app graph (Room DB, repositories, food-list seeding) is wired here as
 * those layers land in later tasks.
 */
class EnoughApplication : Application()
